function changeBackground() {
    
    var text = document.getElementById("items").stylebackgroundColor = "red";
}

function changeBackground() {
    
    var text = document.getElementById("group").stylebackgroundColor = "blue";
    
    function changeBackground() {
   }
    var text = document.getElementById("loc").stylebackgroundColor = "yellow";
   }
    function changeBackground() {
    
    var text = document.getElementById("persons").stylebackgroundColor = "green";
}

/*JJF: These changebackground functions are going to go into the linked list elements
and use their id value, so when the mouse hovers over them, the background behind the 
text will change colors.*/










function backToNormal() {
    
    var text = document.getElementById("loc").stylebackgroundColor = "";
    
}

/*JJF: When the mouse moves off of the changebackground functions, the background
will revert to normal, which is why the stylebackgroundcolor has no value*/